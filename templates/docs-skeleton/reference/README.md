# Reference

Information-oriented pages — dry, accurate facts looked up **while working**: one page per component/service, its interface, configuration, and behavior. Describe, don't explain or instruct. Mirror the code and cite it (`path:line`); never duplicate what a generator (OpenAPI/schema) owns. Start from `_templates/reference-template.md`.
