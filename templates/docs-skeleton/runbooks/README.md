# Runbooks

Operational procedures — specific, copy-pasteable commands for running, deploying, debugging, and recovering the system. **Never put secrets here** — reference the secret manager. Keep them current; a stale runbook is worse than none. Start from `_templates/runbook-template.md`.
